{
  "name": "secret-cloud-storage",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:frontend": "bun build frontend/public/js/app.ts --outfile=frontend/public/js/app.js --target=browser",
    "dev": "bun run build:frontend && bunx wrangler dev --config backend/wrangler.toml",
    "deploy": "bun run build:frontend && bunx wrangler deploy --config backend/wrangler.toml",
    "typecheck": "bunx tsc --noEmit --project backend/tsconfig.json && bunx tsc --noEmit --project frontend/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260122.0",
    "@types/bun": "latest",
    "wrangler": "^4.59.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
